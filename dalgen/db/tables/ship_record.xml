<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE table SYSTEM "http://sources.jungle.net/svn/dtd/table-config-1.0.dtd">

<table sqlname="ship_record">
	<!-- 不需要配置的列请删除 -->
	
	<sql id="ShipRecord.columns">
		id, agent_id, commodity_id, qrcode_no, gmt_create, gmt_modified, create_by, modified_by 
	</sql>
	
	<operation name="insert" paramtype="object">
		<sql>
			INSERT INTO ship_record (
	        	id ,agent_id ,commodity_id ,qrcode_no ,gmt_create ,gmt_modified ,create_by ,modified_by 
	        ) VALUES (
	        	? ,? ,? ,? ,? ,? ,? ,?         
	        )
		</sql>		
	</operation>

	<operation name="delete">
		<sql>
	        DELETE FROM ship_record WHERE
	        id = ? 
		</sql>
	</operation>
	
	<operation name="update" paramtype="object">
		<sql>
	        UPDATE ship_record SET
		        agent_id = ? ,commodity_id = ? ,qrcode_no = ? ,gmt_create = ? ,gmt_modified = ? ,create_by = ? ,modified_by = ? 
	        WHERE 
	        	id = ? 
		</sql>					
	</operation>

	<operation name="queryById" multiplicity="one">
		<sql>
	    	SELECT
		    <include refid="ShipRecord.columns"/>
	        FROM ship_record 
	        WHERE 
		        id = ? 
		</sql>
	</operation>

	
	<operation name="findPage" multiplicity="paging">
		<sql>
			SELECT 
			<include refid="ShipRecord.columns"/>
			FROM ship_record 
		</sql>
	</operation>

</table>
