<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE table SYSTEM "http://sources.jungle.net/svn/dtd/table-config-1.0.dtd">

<table sqlname="role">
	<!-- 不需要配置的列请删除 -->
	
	<sql id="Role.columns">
		id, name, gmt_create, gmt_modified, create_by, modified_by 
	</sql>
	
	<operation name="insert" paramtype="object">
		<sql>
			INSERT INTO role (
	        	id ,name ,gmt_create ,gmt_modified ,create_by ,modified_by 
	        ) VALUES (
	        	? ,? ,? ,? ,? ,?         
	        )
		</sql>		
	</operation>

	<operation name="delete">
		<sql>
	        DELETE FROM role WHERE
	        id = ? 
		</sql>
	</operation>
	
	<operation name="update" paramtype="object">
		<sql>
	        UPDATE role SET
		        name = ? ,gmt_create = ? ,gmt_modified = ? ,create_by = ? ,modified_by = ? 
	        WHERE 
	        	id = ? 
		</sql>					
	</operation>

	<operation name="queryById" multiplicity="one">
		<sql>
	    	SELECT
		    <include refid="Role.columns"/>
	        FROM role 
	        WHERE 
		        id = ? 
		</sql>
	</operation>

	
	<operation name="findPage" multiplicity="paging">
		<sql>
			SELECT 
			<include refid="Role.columns"/>
			FROM role 
		</sql>
	</operation>

</table>
